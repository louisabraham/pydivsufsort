notifications:
  email: false
language: shell
jobs:
  include:
    - os: windows
before_install:
  - choco install python --version=3.8
  - choco install make
install:
  - ./build.sh
  - /c/Python38/python.exe -m pip install .